{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('css/nav.css') }}">
{% endblock %}
<header id="header" role="banner">
    <div class="container">
        <div id="navbar" class="navbar navbar-default">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <div class="navbar-brand" style="margin-top:-1em">
                		<a class="navbar-brand" href="{{ path('admin', {'bip':bip.id}) }}">
                			<div class="navbar-brand-name">
								<img style="max-width: 60px;margin-top:-0.4em;"
                				src="{{ asset('img/bip.png') }}"/>	
                				Biuletyn Informacji Publicznej
                			</div>
                		</a>
                </div>
            </div>
            <div class="collapse navbar-collapse">
                <ul class="nav navbar-nav navbar-right">
                    <li class="{% if app.request.get('_route') 
                    starts with 'admin_add_art' %} active {% endif %}">
                        <a href="{# {{ path('admin_add_art') }} #}">Dodaj artykuły</a></li>
                    <li>
                        <a href="">Edytuj Profil</a></li>
                    <li>
                        <a href="#">Wyloguj</a></li>
                </ul>
            </div>
        </div>
    </div>
</header>