{% extends 'base.html.twig' %}
{% block body %}

    {% include "bip/header.html.twig" %}

<div class="container index-container">
    <div class="index-container">
        <div class="row">
            <div class="col-md-3">
               {% render(controller('AppBundle:BIP:menu', {'bip':bip.id})) %}
            </div>
                <div class="col-md-9">
                    <div class="panel panel-danger">
                        <div class="panel-heading">
                            Zarząd
                        </div>
                        <div class="panel-body">
                            <ul class="list-group panel">
                            {% for org in organy %}
                                <li class="list-group-item row" style="margin-bottom:1.8em;">
                                    <h3>
                                    <div class="col-xs-6">{{ org.organ }}</div>
                                    </h3>
                                </li>
                                <div class="">
                                    <ul class="list-group k">
                                        {% for guy in org.members %}
                                            <li class="list-group-item row">
                                                <div class="col-xs-12">{{guy.firstname}} {{guy.lastname}}</div>
                                            </li>
                                        {% endfor %}
                                    </ul>
                                </div>
                            {% endfor %}
                            </ul>
                        </div>
                        <div class="panel-footer">
                            {% if is_granted('ROLE_BIPADMIN') %}
                                <a href="{{ path('admin_management') }}" class="edit">Edytuj <i class="fa fa-pencil fa-fw"></i></a>
                            {% endif %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}